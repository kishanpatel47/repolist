import 'package:flutter/material.dart';

class ContributeNow extends StatelessWidget {
  const ContributeNow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("HEllo"),
    );
  }
}
